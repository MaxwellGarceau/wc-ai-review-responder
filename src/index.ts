/**
 * External dependencies
 */

/**
 * Internal dependencies
 */
import './index.scss';
import './admin-review-listeners';
import { localizeElements } from './utils/i18n';

// Initialize localization when DOM is ready
document.addEventListener( 'DOMContentLoaded', localizeElements );
